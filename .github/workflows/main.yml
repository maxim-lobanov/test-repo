name: 'test'
on:
  push:
  workflow_dispatch:

jobs:
  job1:
    runs-on: 'macos-latest'
    steps:
    - name: Setup Java JDK
      uses: actions/setup-java@v1
      with:
        java-version: 11.0.10
    
      
