on: [ push, pull_request ]

jobs:

  test1:
    runs-on: macos-latest
    steps:
      - uses: joschi/setup-java@adoptium
        with:
          java-version: '11'
          distribution: 'temurin'
      - run: java -version
