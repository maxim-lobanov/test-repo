on: [ push, pull_request ]

jobs:
  rider:
    name: Rider
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        ls -la $AGENT_TOOLSDIRECTORY/Ruby/2.7.4/x64
        
