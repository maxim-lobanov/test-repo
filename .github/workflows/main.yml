on: [ push, workflow_dispatch ]

jobs:
  rider:
    name: Rider
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
       ls -la /home/runner/runners
       echo "-----"
       ls -la /home/runner/runners/2.284.0/.setup_info
       echo "------"
       cat /home/runner/runners/2.284.0/.setup_info
